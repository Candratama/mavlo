node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Wrangler dev secrets
.dev.vars

# DB dumps
*.sql
remote-*.sql
# ...but Drizzle migrations must be committed — the blanket *.sql rule already
# silently dropped migration 0014 once (D1 ended up missing expense_type).
!drizzle/*.sql

# Claude Code
.claude/
